let
    x = [ (1, c)^.stretched,
          (1, d)^.stretched,
          (1, f)^.stretched,
          (1, e)^.stretched ]^.voice

    y = join $ [ (1, x)^.stretched, 
                 (0.5, up _P5 x)^.stretched, 
                 (4, up _P8 x)^.stretched ]^.voice

in stretch (1/8) $ view (re singleMVoice) . fmap Just $ y
